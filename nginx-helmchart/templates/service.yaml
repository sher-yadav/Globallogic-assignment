apiVersion: v1
kind: Service
metadata:
  name: {{ include "nginx-helmchart.fullname" . }}
  labels:
    {{- include "nginx-helmchart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nginx-helmchart.selectorLabels" . | nindent 4 }}
